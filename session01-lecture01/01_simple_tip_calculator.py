# simple_tip_calculator.py

# --- Greet the user and explain the script's purpose ---
# This provides context for what the user should do next.


# --- Get the total bill amount from the user ---
# We use input() to get the user's response as a string.
# We then convert that string to a float (a number that can have a decimal)
# so we can use it in mathematical calculations.


# --- Define the standard tip percentages ---
# We define these as decimal values for easy multiplication.


# --- Calculate the tip amounts ---
# Multiply the total bill by each tip percentage to find the tip value.



# --- Display the results to the user ---
# We use f-strings (formatted strings) to neatly present the output.
# The ":.2f" inside the curly braces formats the number to have exactly two decimal places,
# which is standard for currency.
